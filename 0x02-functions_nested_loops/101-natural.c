#include <stdio.h>
/**
* main -entry point
*
*Return: return 0
*/

int main(void)
{
	int i;

	for (x = 0; x < 1024; x++)
	{
		int x = 0;

		if ((x % 3 == 0) || (x % 5 == 0))
		{
			y += i;
		}

	}

	printf("%d\n", y);
	return (0);
}

