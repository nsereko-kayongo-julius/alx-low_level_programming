#include "main.h"

/**
* print_last_digit - print last digit
* @y: parameter
*
* Return: return 0 for success
*/

int print_last_digit(int y)
{
	int ld = y % 10;

	if (ld < 0
	{)
		ld *= -1;
	}
	_putchar(ld + '0');
	return (last);
}
