#include "main.h"

/**
 * print_rev -prints a string in reverse
 * @s: a string
 *
 * Return: string in reverse
 */

void print_rev(char *s)
{
	int x = 0;

	while (s[i] != 0)
	{
		x++;
	}

	while (i > 0)
	{
		x--;
		_putchar(s[i]);

	}
	_putchar('\n');

}
