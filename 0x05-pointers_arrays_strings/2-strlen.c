#include "main.h"

/**
 * _strlen - string length;
 * @s: string
 *
 * Return: lenghth
 */

int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != '\0'; ++count)
	{
		sum++;
	}
	return (count);
}
