#include "main.h"

/**
 * _strlen - string length;
 * @s: string
 *
 * Return: lenghth
 */

int _strlen(char *s)
{
	int x, sum = 0;
	char str = str[0];

	for (x = 1; str != '\0'; str++)
	{
		sum++;
	}
	return (0);
}
